server:
  port: 8802
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
spring:
  application:
    name: service-feign
  zipkin:
    base-url: http://localhost:9411
# 配置熔断器
feign:
  hystrix:
    enabled: true

